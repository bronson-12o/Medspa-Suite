# Docker Compose Environment Variables
# These are used by docker-compose.yml for local development

# PostgreSQL Configuration
POSTGRES_USER=dev
POSTGRES_PASSWORD=dev
POSTGRES_DB=medspa_local

# Note: Copy this file to .env if you want to customize values
# The docker-compose.yml will work with the hardcoded defaults above